<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.pc.careuppluss">

    <application
        android:allowBackup="false"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:replace="android:icon,android:theme,android:allowBackup,label">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".welcome" />
        <activity android:name=".signup" />
        <activity android:name=".signup2" />
        <activity android:name=".querypage" />
        <activity
            android:name=".finalpage"
            android:label="@string/title_activity_finalpage"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".discription" />
        <activity android:name=".helps" />
        <activity android:name=".profile" />
        <activity android:name=".rateus" />
        <activity android:name=".tand_c" />
        <activity android:name=".admin_recycle" />
        <activity android:name=".search_by" />
        <activity android:name=".notification" />
    </application>

</manifest>